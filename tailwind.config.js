// Tailwind CSS v4+ ESM config
// Using default color palette (removed custom brand until config verified)
export default {
  content: [
    "./src/web_app/templates/**/*.html",
    "./src/web_app/**/*.py",
    "./src/web_app/static/js/**/*.js"
  ],
  darkMode: 'class',
  theme: {
    extend: {
      // Add customizations here later
    }
  },
  plugins: []
}
